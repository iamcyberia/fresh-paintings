// Cyber was here :3

if(I.isOf(item, Items.get("minecraft:painting"))){
    M.moveY(matrices, 0.025);
    M.moveX(matrices, -0.05);
    M.rotateX(matrices, -7.5);
    M.rotateY(matrices, -5);
}
if(I.isOf(item, Items.get("minecraft:item_frame"))){
    M.moveY(matrices, 0.025);
    M.moveX(matrices, -0.05);
    M.rotateX(matrices, -7.5);
    M.rotateY(matrices, -5);
}
if(I.isOf(item, Items.get("minecraft:glow_item_frame"))){
    M.moveY(matrices, 0.025);
    M.moveX(matrices, -0.05);
    M.rotateX(matrices, -7.5);
    M.rotateY(matrices, -5);
}